# --- Supabase (banco compartilhado com o MRP — tabela public.comercios) ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Obs: o QC NÃO precisa da service_role. O registro de reivindicações/leads
# passa pela função Postgres SECURITY DEFINER `criar_reivindicacao`, que o anon
# pode executar — sem expor chave privilegiada num site público.
