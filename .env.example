# --- Supabase (banco compartilhado com o MRP — tabela public.comercios) ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# server-only — usado pelo server action que registra reivindicações/leads.
# NÃO prefixar com NEXT_PUBLIC. Nunca expor ao browser.
SUPABASE_SERVICE_ROLE_KEY=
